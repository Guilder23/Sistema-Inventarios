// Inicializa el modal de ver producto si estÃ¡ disponible (reusado desde productos)
(function() {
    'use strict';

    if (typeof inicializarModalVer === 'function') {
        inicializarModalVer();
    }
})();
